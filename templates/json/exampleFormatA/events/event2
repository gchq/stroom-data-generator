  {
    "timestamp": "$date.get('yyyy-dd-MM')T$date.get('HH:mm:ss.SSS')Z",
    "action": "Login",
    "user": "$user",
    "host": "$host",
    "fqdn": "$fqdn",
    "deviceip": "$hostip",
    "clientip": "$otherip"  
  }