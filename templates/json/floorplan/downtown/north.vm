  {
    "timestamp": "$date.get('yyyy-MM-dd')T$date.get('HH:mm:ss.SSS')Z",
    "fqdn": "$fqdn",
    "deviceip": "$hostip",
    "action": "Teleport",
    "user": "$otheruser",
    "campus": "Downtown",
    "building": "Overground",
    "floor": "North Tower",
#set ($x =  ($random.nextFloat() * 20) + 10)
#set ($y =  ($random.nextFloat() * 14) + 38)
    "x": $x,
    "y": $y
  }
